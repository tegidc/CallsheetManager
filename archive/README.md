# Archive

Old review pages and mockups from earlier build phases, moved here 15 Aug 2026 so the repo root holds just the app. They still work if opened directly (e.g. `/archive/gate1-review.html` on the live site), but nothing in the app links to them.

MAP.md's history notes refer to these by their old root-level filenames — they now live here.
